# Sourcing colors generated by wallust


general {
    grace = 1
}

background {
    monitor =
	#path = screenshot   # screenshot of your desktop
	path = /home/vladgus/.config/hypr/samurai1.png   # NOTE only png supported for now
    #color = $color7
    # all these options are taken from hyprland, see https://wiki.hyprland.org/Configuring/Variables/#blur for explanations
    blur_size = 0
    blur_passes = 0 # 0 disables blurring
    noise = 0.0100
    contrast = 1.2000 # Vibrant!!!
    brightness = 0.7000
    vibrancy = 0.1900
    vibrancy_darkness = 0.1900
}

input-field {
    monitor =
    size = 100, 30
    outline_thickness = 0
    dots_size = 0.33 # Scale of input-field height, 0.2 - 0.8
    dots_spacing = 0.15 # Scale of dots' absolute size, 0.0 - 1.0
    dots_center = true
    inner_color = rgb(255,255,255)
    fade_on_empty = true
    placeholder_text = <i>Password...</i> # Text rendered in the input box when it's empty.
    hide_input = false
    position = 120, -970
    halign = left
    valign = top
}

# WEATHER 
label {
    monitor =
	    text = cmd[update:3600000] [ -f ~/.cache/.weather_cache ] && cat  ~/.cache/.weather_cache
    color = rgb(184, 192, 224)
    font_size = 10
    font_family = JetBrains Mono Nerd Font Mono ExtraBold
    position = 800, 490
    halign = center
    valign = center
    zindex = 5
}
# DATE
label {
    monitor =
    text = cmd[update:18000000] echo "<b> "$(date +'%A, %-d %B %Y')" </b>"
    color = rgb(113,26,27)
    font_size = 14
    font_family = Noto Nastaliq Urdu
    position = 30, -195
    halign = left
    valign = top
}
# Uptime
#label {
#    monitor =
#    text = cmd[update:60000] echo "<b> "$(uptime -p || $Scripts/UptimeNixOS.sh)" </b>"
#    color = rgb(255,255,255)
#    font_size = 11
#    font_family = JetBrains Mono Nerd Font 10
#    position = 100, -1050
#    halign = left
#    valign = top
#}


# TIME HR
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%H")"
    color = rgb(113,26,27)
    #shadow_size = 3
    #shadow_color = rgb(0,0,0)
    #shadow_boost = 1.2
    font_size = 180
    font_family = JetBrains Mono Nerd Font 10
    position = 20, -250
    halign = left
    valign = top
    zindex = 5
}

# TIME MIN
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%M")"
    color = rgb(255,255,255)
    font_size = 180
    font_family = JetBrains Mono Nerd Font 10
    position = 20, -450
    halign = left
    valign = top
    zindex = 5
}
# TIME SEC
label {
    monitor =
    text = cmd[update:1000] echo -e "$(date +"%S")"
    color = rgb(255,255,255)
    font_size = 20
    font_family = JetBrains Mono Nerd Font 10
    position = 310, -650
    halign = left
    valign = top
    zindex = 5
}

# User
label {
    monitor =
    text =$USER
    color = rgb(255,255,255)
    font_size = 20
    font_family = JetBrains Mono Nerd Font 10

    position = 120, -1010
    halign = left
    valign = top
}
image {
    monitor =
    path = /home/vladgus/.config/hypr/iam.jpg
    size = 75
    rounding = -1
    border_size = 0
    border_color = rgb(184, 192, 224)
    rotate = 0
    reload_time = -1
    position = -790, -385
}
